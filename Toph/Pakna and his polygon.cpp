#include <bits/stdc++.h>
using namespace std;
//Author: Ezio Sam
#define ll long long
#define pb push_back 
#define sp " "
#define s string
#define fr(i,n) for(i=0;i<n;i++)
#define fn(i,n) for(i=1;i<=n;i++)
#define fa(i,a,n) for(i=a;i<=n;i++)

int main()
{
	double n, a, b, c;
	
    cin>>n;
    
    while(n--){
    	cin>>a;
    	
    	b=(3.0*sqrt(3.0))/2.0 ;
    	
    	cout<<fixed<<setprecision (5)<<a*a*b<<endl ;
    }
    
   return 0;
}